---
title: Swift 둘러보기
description: A reference page in my new Starlight docs site.
---

## 간단한 값
- 값은 다른 타입의 값으로 절대 변경되지 않습니다. 
- 값을 다른 타입으로 변경해야 한다면 타입의 인스턴스를 명시적으로 만들어야 합니다.
```swift
let label = "The width is "
let width = 94
let widthLabel = label + String(width)
let widthLabel = label + width // ❌ Error: Binary operator '+' cannot be applied to operands of type 'String' and 'Int'
```
<br/>

## String
- 문자열에 값을 포함하는 방법은 \\() 안에 값을 작성하면 됩니다.
```swift
let apples = 3
let oranges = 5
let appleSummary = "I have \(apples) apples."
let fruitSummary = "I have \(apples + oranges) pieces of fruit."
```


## Reference
- [Swift org](https://bbiguduk.gitbook.io/swift/welcome-to-swift/swift-a-swift-tour)
